version: 2
jobs:
  build:
    docker:
      - image: circleci/node:8
    working_directory: ~/functions
    steps:
      - checkout
      - run: npm install
      - run: npm run build
      # - run: ionic serve -bd&
      # - run: npm run e2e -- --no-progress